[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "psutil>=7.0.0",
    "requests>=2.32.4",
    "websockets>=15.0.1",
]
